// @flow

import * as React from 'react'
import barMenu from 'data/bar'
import Menu from '../Menu'

const Bar = () => {
  return (
    <Menu menu={barMenu} sectionType="half" />
  )
}

export default Bar
